package inOutput;

public class test {
	/*
	 * Scanner scanner = new Scanner(System.in())					| 
	 * scanner.nextLine()											| Leest de volledige lijn
	 * scanner.next()												| Leest tot de volgende spatie. (Tot de spatie)
	 * scanner.nextInt()											| Leest de int
	 * scanner.nextDouble()											| Leest de double.
	 * scanner.hasNext() ->( en andere, hasNextInt(), ....)			|
	 */
}
